CREATE TABLE `noqbites`.`restaurant` (
  `restaurant_id` INT NOT NULL AUTO_INCREMENT,
  `address` VARCHAR(50) NOT NULL,
  `password` VARCHAR(50) NOT NULL,
  `restaurant_name` VARCHAR(50) NOT NULL,
  PRIMARY KEY (`restaurant_id`));